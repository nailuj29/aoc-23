import { assertEquals } from "https://deno.land/std@0.208.0/assert/mod.ts";
import {loadTestData, loadData } from "./utils.ts";

//const part1test = // fill this in
// const part2test = // fill this in

const part1 = (data: string) => {
    // fill this in
};

const part2 = (data: string) => {
    // fill this in
};

assertEquals(part1test, part1(loadTestData(1)))
console.log(part1(loadData(1)))
// assertEquals(part2test, part2(loadTestData(1)))
// console.log(part2(loadData(1)))
